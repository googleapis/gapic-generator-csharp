syntax = "proto3";

package example;

service Example {
    rpc ExampleMethod(Request) returns(Response);
}

message Request {
    string name = 1;
    optional int32 number = 2;
}

message Response {
}
