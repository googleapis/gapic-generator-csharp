syntax = "proto3";

package testing;

import "google/api/annotations.proto";

service Basic {
  rpc IdempotentMethod(Request) returns(Response) {
    option (google.api.http).get = "/path/here";
  };
  rpc NonIdempotentMethod(Request) returns(Response);
}

message Request {
}

message Response {
}
